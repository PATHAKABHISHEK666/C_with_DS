## 1. Formatted Marksheet using `printf()`
Write a C program to print final exam report card using the printf function. 
the report should display the student's details and marks in different subjects in a neat tabular format.
---
## 2. Grass Cutting Time Calculation
Write a C program that takes as input the length and breadth of a rectangular yard and the length and breadth of a rectangular house situated inside the yard.  
Compute the time required to cut the grass at the rate of **2 square feet per second**.
---
## 3. Population Growth Prediction
Write a C program to compute the **rate of growth** as a **percentage of population**.  
- Input: initial population and population after one week.  
- Predict the population after another week, assuming constant growth.
---
## 4. Fraction Multiplication and Percentage Equivalent
Write a C program to input the **numerator and denominator** of two fractions and:
- Compute their **product**  
- Display the resulting numerator and denominator  
- Display the **percentage equivalent**

---

## 5. Rounding a Floating Number
Write a C program that takes a **positive number with a fractional part** and rounds it to **two decimal places**.  
Example:  
`Input: 32.4851 → Output: 32.49`
---

## 6. Employee Salary Calculation
Write a C program to calculate salary based on hours worked:  
- If hours ≤ 40 → normal hourly rate  
- If hours > 40 → overtime at 1.5× normal rate  
Display the total salary.
---

## 7. Sales Commission Calculation
Write a C program to calculate the **commission** of a salesman:  
- Sales < 1000 → No commission  
- Sales between 1000 and 10,000 → 10% of sales  
- Sales > 10,000 → 1000 + 8% of (sales - 10,000)
---

## 8. Long-Distance Call Billing
A telephone company charges:
- Regular rate: ₹0.40/minute  
- After 6 PM & before 8 AM → 50% discount  
- Calls > 60 minutes → additional 15% discount  
- All calls → 40% federal tax applied after discount  

Write a program to compute and display **gross** and **net** cost.

---

## 9. Electricity Bill Calculation
Write a C program to print bills for a city power company.  
Use codes:
- `R` – Residential  
- `C` – Commercial  
- `I` – Industrial  

**Rates:**
- Residential: ₹6 + ₹0.052/kWh  
- Commercial: ₹60 for first 1000 kWh + ₹0.045/additional kWh  
- Industrial:  
  - Peak: ₹76 + ₹0.065/additional kWh  
  - Off-peak: ₹40 + ₹0.028/additional kWh  

Prompt for **account number**, **code**, and **units used**, then display **amount due**.

---

## 10. Series Using Loops
Write C programs to generate the following series:
1. `1 + 4 + 25 + 36 + ... + n`  
2. `a + 1 + b + 2 + c + 3 + ... + n`  
3. `Za + Yb + Xc + ... + aZ`  
4. `s = 1 + 2*1 + 3*2 + ... + n*(n-1)`

---

## 11. Odd Numbers Between Two Values
Write a C program to input two numbers and display  **odd numbers** between them.

---

## 12. Star Pattern Display
Display the following pattern using C:

1.

        *
      * * *
    * * * * *
  * * * * * * *

2.
   
        *
      * * *
    * * * * *
  * * * * * * *
    * * * * * 
      * * *
        *

3.

  
  A 
  A B 
  A B C 
  A B 
  A




---

## 13. Power Function (Float Raised to Integer Power)
Write a function to evaluate `y = x^d`,  
where **x** and **y** are floating variables and **d** is an integer.

---

## 14. Swapping Two Values using Function and Pointer
Write a C program to **swap the values** of two variables using a **function** and **pointers**.

---

## 15. Set Smaller Value to Zero (Function + Pointer)
Write a C program using a function and pointers to compare two numbers and **set the smaller one to zero**.

---

## 16. Convert Number to Negative (Function + Pointer)
Write a C program to convert a number to its **negative form** using a **function** and **pointer**.

---

## 17. Add Least Significant Digit (Function)
Write a C program to **extract and add the least significant digit** of an integer using a **function**.

---

## 18. Maximum and Minimum in Array
Write a C program to:
- Read elements into an array  
- Display all elements  
- Find and print the **maximum** and **minimum** values

---

## 19. Average of Array Elements
Write a C program to:
- Read elements into an array  
- Display all elements  
- Find and print the **average value**

---

## 20. Check for Duplicates in Array
Write a C program to:
- Read elements into an array  
- Display all elements  
- Check for **duplicate values**  

If duplicates exist → print `"Duplicate values exist"`  
Else → print `"No duplicate values"`

---

## 21. Copy Array Elements
Write a C program to:
- Read elements into an array  
- Copy them into another array  
- Display both arrays

---

## 22. Remove Duplicates and Copy Unique Elements
Write a C program to:
- Read elements into an array  
- Remove duplicates  
- Copy only unique elements into another array  
- Display the second array

---

## 23. Linear Search in Array
Write a C program to **perform a linear search** for a given element in an array.

---

## 24. Bubble Sort in Ascending Order
Write a C program to **sort an array** in **ascending order** using the **Bubble Sort** technique.

---

## 25. Matrix Subtraction
Write a C program to:
- Read two matrices of the same order (same number of rows and columns)
- Perform **matrix subtraction**
- Display the resulting matrix

Example:  
If  
A = |1 2|  
    |3 4|  
and  
B = |4 3|  
    |2 1|  

Then  
Result = |−3 −1|  
         |1 3|

---

## 26. Matrix Multiplication
Write a C program to:
- Read two matrices (A and B)
- Check if multiplication is possible (columns of A = rows of B)
- Perform **matrix multiplication**
- Display the resulting matrix

Example:  
If  
A = |1 2|  
    |3 4|  
and  
B = |2 0|  
    |1 2|  

Then  
Result = |4 4|  
         |10 8|

---

## 27. Matrix Transpose
Write a C program to:
- Read a matrix of order `m × n`
- Find its **transpose** (interchanging rows and columns)
- Display the **original matrix** and its **transpose**

Example:  
If  
A = |1 2 3|  
    |4 5 6|  

Then  
Transpose(A) = |1 4|  
               |2 5|  
               |3 6|

